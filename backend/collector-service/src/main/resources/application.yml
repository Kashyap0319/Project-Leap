server:
  port: ${SERVER_PORT:8080}

spring:
  data:
    mongodb:
      uri: ${LOGS_MONGO_URI:mongodb://localhost:27017/logsdb}

mongo:
  logs:
    uri: ${LOGS_MONGO_URI:mongodb://localhost:27017/logsdb}
  meta:
    uri: ${META_MONGO_URI:mongodb://localhost:27017/metadb}

security:
  jwt:
    secret: ${JWT_SECRET:7fbdc81f7e5a46b8a9f5258a9a6ec8da58be0e337a0cc8f5c19a43512e8c7f26}
    issuer: api-monitoring

jwt:
  secret: ${JWT_SECRET:7fbdc81f7e5a46b8a9f5258a9a6ec8da58be0e337a0cc8f5c19a43512e8c7f26}
  expiration: 86400000

rate-limit:
  default: 100

monitoring:
  rateLimit:
    default: 100

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,https://leapproject.vercel.app}

logging:
  level:
    root: ${LOG_LEVEL:INFO}
    org.springframework.security: ${LOG_LEVEL:INFO}
    org.springframework.web: ${LOG_LEVEL:INFO}
    com.projectleap: ${LOG_LEVEL:INFO}
